#include <iostream>
#include <string>

int main(int argc, char* argv[]) {
    std::cout << "================================================" << std::endl;
    std::cout << "   ML Inference Benchmarking Tool v1.0" << std::endl;
    std::cout << "   Built with MSVC + CMake on Windows" << std::endl;
    std::cout << "================================================" << std::endl;
    std::cout << "\n[INFO] Build pipeline verified successfully." << std::endl;
    std::cout << "[INFO] Ready to load ONNX models." << std::endl;
    return 0;
}